# Base URL of the FastAPI backend.
#
# NEXT_PUBLIC_ is required: client components read this after bundling, where
# only inlined public vars exist. It is a URL, not a secret — do not put the
# Rox API token here; that stays server-side in backend/.env.
NEXT_PUBLIC_API_URL=http://localhost:8000
